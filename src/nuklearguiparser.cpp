#include <iostream>
#include <fstream>

#include "nuklearguiparser.h"

namespace nkcpp
{
    NuklearGUIParser::NuklearGUIParser()
    {
      /*  ifstream input;

        input.open( "gui.json" );
        if (input.is_open())
        {
            std::string line;
            while ( getline (input, line) )
            {
                cout << line << '\n';
            }
            input.close();
        }*/
    }
}
